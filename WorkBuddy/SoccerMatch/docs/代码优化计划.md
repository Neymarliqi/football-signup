# 代码优化计划

## 当前状态

### 已完成
- ✅ 创建了公共工具模块 `utils/constants.js`
- ✅ 创建了公共工具模块 `utils/format.js`
- ✅ 创建了公共工具模块 `utils/request.js`

### 待优化项

#### 1. 首页 (pages/index/index.js)
**冗余代码：**
- `posMap` 位置映射表 (行 179-186)
- `formatDate` 函数 (行 261-267)
- `getFirstPosition` 函数 (行 189-213)

**优化方案：**
- 保留 `requestWithRetry` 函数（行 47-61）- 已在 format.js 中
- 删除本地定义的 `posMap`
- 删除本地定义的 `formatDate`
- 删除本地定义的 `getFirstPosition`
- 使用公共模块中的函数

**影响范围：**
- `formatActivity` 函数需要改用公共函数
- 需要测试活动列表加载和显示

#### 2. 详情页 (pages/activity/detail.js)
**冗余代码：**
- `posMap` 位置映射表
- `formatDate` 函数
- `getFirstPosition` 函数

**优化方案：**
- 使用公共模块中的函数

#### 3. 战术板 (pages/tactics/tactics.js)
**冗余代码：**
- `posMap` 位置映射表

**优化方案：**
- 使用公共模块中的函数

#### 4. 个人中心 (pages/profile/profile.js)
**冗余代码：**
- `posMap` 位置映射表

**优化方案：**
- 使用公共模块中的函数

#### 5. 云函数优化
**待删除的云函数：**
- `cloudfunctions/debugAdmin` - 调试用，生产环境不需要
- `cloudfunctions/fixAdminOpenid` - 一次性修复脚本，已完成

**注意：** 删除前需要确认无引用

#### 6. 数据库查询优化
**问题：**
- `profile.js` 中 `loadHistory()` 使用 `.get()` 加载所有活动，无分页

**优化方案：**
- 添加 `.limit(20)` 限制返回数量
- 或添加分页支持（下一页加载）

#### 7. 冗余变量
**问题：**
- `detail.js` 中同时有 `defaultAvatarUrl` 和 `placeholderAvatar`

**优化方案：**
- 统一使用一个变量名

#### 8. 组件提取（可选）
**可提取的公共组件：**
- 活动卡片组件（首页、历史页面、管理后台）
- 球员头像组件（详情页、战术板）

**注意：** 组件提取是大改动，不建议立即进行

## 优化步骤

### 第一步：更新 utils/constants.js 和 utils/format.js
- 确保公共工具函数完整、可用
- 包含所有需要的位置映射、状态映射、格式化函数

### 第二步：逐个页面替换重复代码
1. 更新 `pages/index/index.js`
2. 更新 `pages/activity/detail.js`
3. 更新 `pages/tactics/tactics.js`
4. 更新 `pages/profile/profile.js`

### 第三步：删除未使用的云函数
1. 确认 `debugAdmin` 无引用
2. 确认 `fixAdminOpenid` 无引用
3. 删除云函数

### 第四步：全面测试
1. 测试首页功能（活动列表、筛选、卡片点击）
2. 测试详情页功能（报名、取消报名、位置显示）
3. 测试战术板功能（位置显示）
4. 测试个人中心功能（历史记录、位置选择）
5. 测试管理后台功能（删除活动、设置管理员）

### 第五步：更新文档
- 更新 `MEMORY.md` 记录优化变更
- 更新 `开发说明.md`（如需要）

## 风险控制

1. **保留备份**
   - 优化前备份项目
   - 每个页面优化前备份页面文件

2. **逐步测试**
   - 每完成一个页面优化，立即测试
   - 确保无 Bug 后继续下一个页面

3. **回滚方案**
   - 如果出现问题，立即回滚到优化前版本
   - 使用 Git 版本控制（如已初始化）

## 预期收益

1. **代码质量提升**
   - 消除重复代码
   - 统一代码风格
   - 提高可维护性

2. **性能提升**
   - 减少代码体积
   - 加载速度提升

3. **可维护性提升**
   - 修改位置映射只需改一处
   - 修改日期格式化只需改一处

## 时间估算

- 第一步：30 分钟
- 第二步：2 小时（每个页面 30 分钟）
- 第三步：15 分钟
- 第四步：1 小时
- 第五步：15 分钟

**总计：约 4 小时**

## 执行建议

由于这是一个较大规模的重构，建议：

1. 在测试环境中先完成
2. 确认无误后再同步到生产环境
3. 可以分批次完成，不要一次性全部修改

---

*创建时间：2026-03-25*
*状态：待执行*
