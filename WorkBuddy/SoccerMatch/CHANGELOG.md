# 约球助手更新日志

## v2.1.1 (2026-03-26)

### 微信审核合规整改 — "先浏览，后注册"

- **移除所有页面 onShow 中的注册拦截**：首页/详情/创建/个人中心均不再自动弹出注册弹窗，用户可自由浏览所有内容
- **注册触发时机延后**：仅在执行需要身份的操作时才检查注册（报名/待定/请假/发布活动/编辑资料/位置偏好）
- **注册弹窗增加关闭按钮**：右上角 ✕ 关闭 + 点击遮罩层关闭，用户可拒绝注册继续浏览
- **注册后自动继续操作**：注册成功后自动执行用户之前触发的操作（如报名→弹出活动描述确认→确认报名）
- **TabBar 发布按钮**：在首页时通过 `getCurrentPages()` 直接弹窗，避免 `switchTab` 不触发 `onShow` 的问题；非首页时设置全局标记由首页检测
- **活动描述确认弹窗**：修复首次用户注册后未弹出活动描述确认弹窗的问题（`data-action` 值为 `confirmed` 而非 `confirm`）

---

## v1.1.3 (2026-03-25)

### 战术板页面优化

#### 多人报名场景支持
- **替补区域可滚动**：使用 `scroll-view`，支持40+人报名场景
- **保存按钮固定底部**：始终可见，不会被长列表推到下面
- **统计信息显示**：分隔线显示"已选 X / 总人数"
- **渐变背景优化**：底部按钮区添加渐变，防止文字重叠

#### 球员显示样式统一
- **头像尺寸**：60rpx × 60rpx（场上球员和替补球员统一）
- **昵称显示**：最多8个字符，字体大小15rpx，最大宽度100rpx
- **位置标签**：字体大小10rpx
- **替补球员排列**：行间距24rpx，列间距40rpx（紧凑不拥挤）

#### 拖拽体验优化
- **精确跟随手指/光标**：元素左上角直接对齐触摸位置，无任何偏移
- **性能优化**：移除复杂的尺寸计算，直接使用触摸坐标

---

## v1.1.2 (2026-03-25)

### 云函数新增
- **deleteActivity**：通过云函数删除活动，绕过客户端权限限制
- **setAdmin**：设置/取消管理员权限

### 管理员权限设计
- 使用单独的 `admins` 集合存储管理员 openid
- `app.js` 中的 `checkAdmin` 函数查询 `admins` 集合判断权限
- 存储完整微信 openid（非短ID）

### 微信头像与昵称获取修复
- 修复个人中心点击头像/昵称时，选择弹窗显示不正确的问题
- 改用 `app.globalData.userInfo` 或 `wx.getStorageSync('userInfo')` 获取已保存的用户信息

---

## v1.1.1 (2026-03-23)

### 隐私保护
- 新增隐私保护指引页面（`pages/privacy/privacy`）
- 个人中心增加「设置」菜单，包含隐私保护指引入口
- 优化隐私页面样式，采用卡片式布局

### 数据更新体验优化
- **移除下拉刷新**：首页和个人中心关闭下拉刷新
- **智能刷新策略**：
  - 首页：30秒间隔判断 + 数据库实时监听（最近7天活动）
  - 活动详情：精确监听单个活动变化
  - 个人中心：本地缓存优先，后台静默同步
- **性能优化**：减少不必要的数据库查询

---

## v1.1.0 (2026-03-22)

### UI/UX 全面优化
- **视觉层次分明**：主操作 → 功能操作 → 分隔线 → 管理操作
- **管理操作弱化**：灰色文字、无背景、无icon
- **左对齐原则**：所有操作元素左对齐
- **统一间距**：推荐 12rpx gap

### 首页优化
- 卡片按钮区：主操作→功能操作→分隔线→管理操作
- 删除悬浮发布按钮（TabBar已有）
- 修复取消活动功能（阻止事件冒泡+详细权限检查）

### 活动创建优化
- **赛制类型**：非必填，默认"友谊赛"，支持自定义
- **导航地址**：非必填，调用地图选择位置
- **最大人数**：支持"不限人数"切换
- **报名截止时间**：预设选项（1天/6小时/1小时/0.5小时/不限制）
- **自定义赛制**：每个活动独立存储

### 活动详情优化
- 一步获取用户信息（wx.getUserProfile）
- 自动完成报名
- 简化"待定/请假"逻辑
- 去除确认弹窗

### 自定义 TabBar
- 纯 CSS 绘制图标（房子、人像、加号）
- 未选中：灰色(#999)
- 选中：微信绿(#07c160)，填充浅绿色背景
- 发布按钮：绿色圆形背景 + 白色加号
- 修复导航切换问题（parseInt索引 + 避免重复切换）

---

## v1.0.0 (2026-03-22)

### 初始版本发布
- 基础活动发布功能
- 活动列表展示
- 报名/取消报名
- 个人中心
- 战术板基础功能
